import { Menu } from 'antd';
import { MenuGroup } from '../styles';

function DirectMessages() {
  return (
    <Menu theme="dark">
      <MenuGroup title="Direct Messages"></MenuGroup>
    </Menu>
  );
}

export default DirectMessages;
